{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Connect to debug container",
            "type": "go",
            "request": "attach",
            "mode": "remote",
            "substitutePath": [{
                "from": "${workspaceFolder}/src/k8s",
                "to": "/root/parts/k8s-binaries/build"
            }],
            "port": 2345,
            "host": "127.0.0.1"
        }
    ]
}
